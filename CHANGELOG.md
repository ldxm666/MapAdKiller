# Changelog

## v1.2.2

### 新增

- **榜单板块结构识别（boardSweep）**：目的地 / 路线详情页的「发现好去处」等推荐板块，
  标题为 GL 级渲染（不走任何文本钩子），改用纯结构判据 —— 满屏容器 + 子树 ≥2 张
  460~560 宽双列卡 → 整块隐藏。所有双列卡板块一网打尽，`feed_board` 统一开关管。
- **主动挂列表 bind hook（sweepHookLists）**：目的地 / 路线详情页的 AJX 列表
  不再依赖 collapseHost 先命中，applyPass 直接挂 adapter hook。
- **区块兜底（collapseBlock）**：非列表页面的跨页规则从板块标题上溯到
  「宽≥60%屏、高≤80%屏」的最近祖先整块收掉。

## v1.1.4 ~ v1.1.9

### 修复

- **工具格隐藏改「幽灵格」三件套**：GONE 在 AJX 上有两个死症（自定义命中检测
  不查可见性 → 隐藏格触摸区还在，点空白进出行节页；模型重建拉锯 → 乱闪）。
  改为保持 VISIBLE + alpha 0 + 子树 GONE + 触摸吞掉，三害齐除。
- **宫格重排与 AJX 布局拉锯**：撤 tick 级复排，重排退回事件驱动；
  新增「工具宫格自动排序」实验性开关（默认关）。
- **更多工具点不动**：AJX 容器触摸分发不跟随 translation，重排改回 layout()。
- 跨行重排坐标修正、轮播双实例去重、出行节轮播第二格子树 CD 兜底。

### 新增

- 「秒送」工具识别（17.x 新工具）
- 顶部运营横幅 / AI叫车卡强制清除；悬浮推广球窗口级扫描
- 榜单/特色推荐卡统一开关（feed_board，跨页面生效）

## v1.1.0

### 新增

- **液态玻璃设置页**（设计参考 Kyant0/AndroidLiquidGlass）：光斑背景、玻璃卡片、
  可折叠抽屉、底部悬浮操作栏、深色模式自适应
- **配置管理**：保存 / 恢复 / 导出 / 导入
- **联系作者**：Telegram 群组 + GitHub Issues
- **隐藏桌面图标后 LSPosed 管理器入口常驻**（CATEGORY_INFO 通道，与桌面完全正交）

## v1.1.1

### 修复

- **宫格重排与 AJX 布局拉锯导致持续乱闪**：撤掉 tick 级复排，重排退回事件驱动
  （onTextSet → applyPass）；新增「工具宫格自动排序」开关（默认开，出问题可关）。
- **更多工具点不动**：AJX 自定义容器的触摸分发不跟随 translation 平移，
  重排改回 layout() 直改几何（触摸即绘制位置）。
- **「高德出行节」轮播第二格漏网可点**：packGrid 收集时做子树 contentDescription
  深查（CD 常挂在深层子节点，单点检查会漏）。
- **跨行重排坐标算错**：getTop() 相对各自父行，跨行搬运必须补「目标行 − 源行」
  的 top 差，否则格子飘到错误行。
- **轮播同键双实例重影**（更多工具×2）：同键第二实例（未显示的）GONE 掉。

### 新增

- 「秒送」工具识别与开关（17.x 新工具，大字「送」别名归一到「秒送」键）
- 顶部运营横幅强制清除（十一出行补贴 / 限时开领 / 赢好礼 / AI叫车 / 一句话定制）
- 悬浮推广球窗口级扫描（floatBallSweep：扫街榜 / 赢好礼 文案 → 宿主分支 GONE）

## v1.1.0

### 新增

- **液态玻璃设置页**（设计语言参考 Kyant0/AndroidLiquidGlass）：
  柔和光斑背景、玻璃卡片、可折叠抽屉分组（默认收起）、底部悬浮操作栏、
  胶囊开关、深色模式自适应
- **配置管理**：保存配置 / 恢复配置 / 导出配置（系统分享）/ 导入配置（粘贴 JSON）
- **联系作者入口**：Telegram 群组 + GitHub Issues
- **隐藏桌面图标后 LSPosed 管理器入口常驻**：MainActivity 挂
  `ACTION_MAIN + CATEGORY_INFO` 通道 —— 桌面启动器只认 CATEGORY_LAUNCHER、
  永不展示 INFO 入口，而 LSPosed 管理器的 getLaunchIntentForPackage 优先命中
  INFO，两边从此完全正交（真机验证：alias 禁用后桌面查询返回空、INFO 启动正常）

## v1.0.8

### 修复

- **「我的」页红包答题卡删不掉**：它整张是图、没有任何文本控件，文案匹配永远打不到。
  改用结构判据（子树里没有 AJX 文本控件 + 占满整宽 + 有图）识别，只删它、不碰其它卡片。
- **搜索栏下快捷入口删不干净 / 右滑还有**：那一排是横向分页器，只摘「行」第二页照样滑出来。
  现在从格子往上找到 pager 整块摘除，两页与页码指示点一起消失；默认关闭（设置页可开）。
- **卡顿**：去掉每帧全树扫描（改为只重申已隐藏节点）、绑定路径不再跑整树 BFS、
  正则预编译、资源名按 id 缓存；实测首页来回猛滑 10 次 2778 帧仅 7 帧掉帧（0.25%）。
- **闪烁**：AJX 的文字是绑定返回之后才写进去的，旧实现只能下一帧判 → 广告先亮一下。
  现在绑定前先挂 alpha 0，在首帧绘制之前判完（广告 GONE / 正常卡片同帧恢复）。
- **路线规划页被误删**：所有首页规则现在只在「已认领」的首页 /「我的」页列表内生效，
  路线规划页整页不再受影响。

### 新增

- 隐藏桌面图标开关（快捷设置磁贴 + `adb shell am start -n io.github.ldxm666.mapadkiller/.MainActivity` 双入口）
- 搜索栏下快捷入口总开关（默认关闭）
- 「好友动态」与「答题瓜分百万大奖」红包卡强制清除（无开关）
- 工具栏「高德出行节」强制清除（无开关）

### 修复（续）

- 底部标签栏删掉 tab 后悬浮胶囊没被占满 → 胶囊随槽位自适应
- 百度地图 22.0.0 卡开屏（不再强改混淆闸门返回值 + 开屏 watchdog 兜底放行）
- 补齐 manifest 里声明却缺失的 `LearnedReceiver`

## v1.0.7

- 广告 SDK 特征表扩充（tradplus / klevin / windmill / anythink / beiron 等新版聚合）
- 状态显示改为向 LSPosed 服务查询作用域，红绿点可验证

## v1.0.6

- 广告 SDK 自动检索 + 学习 + 持久化（扫目标 App 自身 dex 字符串，ContentProvider 通道落盘）
- 修复框架类被全局 hook 导致百度闪退 / 腾讯 ANR
- 高德首页内容流改为绑定即隐藏

## v1.0.5

- 高德首页 /「我的」页 UI 自定义（标签栏、工具宫格、推荐内容、我的页各栏）
- 修复一条推荐区规则把整个工具宫格抹掉

## v1.0.4 / v1.0.3 / v1.0.2 / v1.0.1 / v1.0.0

- v1.0.3：修复 `Hooker.intercept` 方法名错误导致全部 Hook 失效（AbstractMethodError）
- v1.0.2：修复百度开屏白屏；v1.0.0：首版（高德/百度/腾讯 去广告）
